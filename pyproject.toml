[project]
name = "ai-civ"
version = "0.1.0"
description = "A terminal-based AI civilization simulation driven by local LLMs"
authors = [
    { name = "gaurav", email = "gauravyadav122142@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain-core>=1.4.0",
    "langchain-ollama>=1.1.0",
]

[tool.uv]
package = false
